import { Button } from "./ui/button";

const BottomBar = () => {
    return (
        <div className="w-full h-20 bg-gradient-to-t from-stone-200 to-stone-50 absolute left-0 bottom-0"></div>
    );
};

export default BottomBar;
